{
  "name": "employee-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "mysql db/employeesDB.db < db/schema.sql",
    "seed": "mysql db/employeesDB.db < db/seeds.sql",
    "db": "mysql db/employeesDB.db",
    "watch": "npx nodemon server.js",
    "start": "node server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sclebron/employee-tracker.git"
  },
  "author": "Sophie LeBron",
  "dependencies": {
    "inquirer": "^8.2.4",
    "mysql2": "^2.2.5",
    "console.table": "^0.10.0"
  }
}
